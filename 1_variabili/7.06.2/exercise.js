let hello = 'Ciao';     // number
let age = 18;       // boolean
let isGraduated = false;     // string


console.log(Number(hello));   // NaN perchè non è quantificabile in numeri
console.log(Boolean(age));     // qualunque valore che non sia 0 o indefinito è true
console.log(String(isGraduated));
